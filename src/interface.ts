interface Action {
    x: number,
    y: number,
    w: number,
    h: number,
}

interface ActionXArr {
    x: number[]
    y: number,
    w: number,
    h: number,
}

interface ActionXWArr {
    x: number[]
    y: number,
    w: number[],
    h: number,
}